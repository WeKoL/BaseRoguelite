using System.Collections.Generic; public sealed class BaseUpgradeDefinition { private readonly List<CraftIngredient> _cost=new(); public string Id{get;} public string DisplayName{get;} public IReadOnlyList<CraftIngredient> Cost=>_cost; public BaseUpgradeDefinition(string id,string name,IEnumerable<CraftIngredient> cost){Id=id??"";DisplayName=string.IsNullOrWhiteSpace(name)?Id:name;if(cost!=null)_cost.AddRange(cost);} }